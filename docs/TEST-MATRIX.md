# Матрица проверок

Состояние: локальный сценарий с подтверждённым mock-удалением и real read-only адаптером реализован для сделок и лидов. Настоящий портал не подключался. Статус строки ставится PASS только если полностью подтверждён её ожидаемый результат в указанной среде; scoped evidence ниже не превращает незавершённые сценарии удаления или интеграции в PASS.

## Обозначения и порядок

НЗ — не запускалось. После выполнения указать PASS, FAIL или BLOCKED и ссылку на запись прогона. N/A допустимо только с причиной.

Mock — локальные искусственные данные без сети. Bitrix — испытание на точном согласованном портале и наборе ID. Сначала проходят соответствующие mock-проверки, затем разрешённые интеграционные тесты. Искусственно перегружать API или создавать массовые fixtures без разрешения нельзя.

## Сценарии

| ID  | Проверка                                                     | Ожидаемый результат                                                               | Mock                               | Bitrix |
| --- | ------------------------------------------------------------ | --------------------------------------------------------------------------------- | ---------------------------------- | ------ |
| T01 | Наборы 0, 1, 50, 51, 3 000 и 3 001                           | Правильные страницы, нет дублей; превышение лимита требует сузить фильтр          | PASS — mock-preview-2026-09-10     | НЗ     |
| T02 | Пустой фильтр, ноль и незавершённый сбор                     | DELETE не отправляется                                                            | PASS — local-operation-2026-09-10  | НЗ     |
| T03 | Изменение фильтра/исключений после подтверждения             | Старое подтверждение аннулировано                                                 | PASS — local-operation-2026-09-10  | НЗ     |
| T04 | Новая подходящая запись после preview                        | Её ID не добавлен в подтверждённый набор                                          | PASS — local-operation-2026-09-10  | НЗ     |
| T05 | Исключение нужной карточки                                   | Для исключённого ID нет команды DELETE                                            | PASS — local-operation-2026-09-10  | НЗ     |
| T06 | Выбор страницы/всего списка и поиск в таблице                | Счётчики понятны, поиск не меняет выбор                                           | НЗ                                 | НЗ     |
| T07 | Двойной клик и две вкладки                                   | Одна операция не исполняется параллельно в поддерживаемом контексте               | PASS — local-operation-2026-09-10  | НЗ     |
| T08 | Смена портала/пользователя/типа                              | Чужая операция не показывается как своя и не запускается                          | НЗ                                 | НЗ     |
| T09 | Изменение карточки после preview                             | Обнаруженное изменение пропущено; невозможность проверки не считается успехом     | НЗ                                 | НЗ     |
| T10 | Частично успешный пакет                                      | Результаты и счётчики соответствуют отдельным ID                                  | PASS — local-operation-2026-09-10  | НЗ     |
| T11 | Ответ потерян после отправки                                 | Неизвестный исход, сверка; нет слепого повтора пакета                             | PASS — local-operation-2026-09-10  | НЗ     |
| T12 | Карточка отсутствует или недоступна                          | Нет ложного «удалено Cleaner»                                                     | НЗ                                 | НЗ     |
| T13 | Сеть/REST-лимит/временная ошибка                             | Ограниченные повторы, понятное ожидание; тест лимита сначала через mock           | НЗ                                 | НЗ     |
| T14 | Нет прав, истекла/отозвана авторизация                       | Безопасная пауза/ошибка; нет бесконечных повторов и ложного нуля                  | PASS — local-operation-2026-09-10  | НЗ     |
| T15 | IndexedDB недоступна или запись в неё отказала               | Новые удаления не отправляются; неизвестность уже отправленных учтена             | PASS — local-operation-2026-09-10  | НЗ     |
| T16 | Закрытие до/после отправки и повторное открытие              | Нет автостарта; сверка и явное продолжение сохранившейся операции                 | НЗ                                 | НЗ     |
| T17 | Пауза и отмена во время запроса                              | Новые отправки прекращаются; завершение отправленных учтено                       | PASS — local-operation-2026-09-10  | НЗ     |
| T18 | Очистка данных браузера/смена устройства                     | Утрата локальной истории объяснена, восстановления не выдумываем                  | НЗ                                 | НЗ     |
| T19 | Обновление схемы/версии приложения                           | Данные мигрируют корректно; старые операции не стартуют сами                      | НЗ                                 | НЗ     |
| T20 | HTML/XSS в названиях, опасные ссылки, CSV-формулы            | Безопасный вывод/переходы/экспорт, правильные кавычки и кириллица                 | PASS — readonly-adapter-2026-09-10 | НЗ     |
| T21 | Связанные данные и корзина                                   | Реальные последствия документированы; непроверенные опасные случаи ограничены     | НЗ                                 | НЗ     |
| T22 | Вход GET/POST, пути assets, iframe, SDK                      | Приложение открывается, права и контекст доступны, секреты не попадают в логи     | НЗ                                 | НЗ     |
| T23 | Второй портал и заявленные браузеры                          | Независимость установок и работа локального состояния проверены                   | НЗ                                 | НЗ     |
| T24 | React StrictMode, повторный mount и render                   | DELETE не запускается из render/Effect; подтверждённая операция не дублируется    | PASS — local-operation-2026-09-10  | НЗ     |
| T25 | Некорректные типы и структура ответа API                     | Runtime-проверка отклоняет данные; опасная команда не строится по догадке         | PASS — readonly-adapter-2026-09-10 | НЗ     |
| T26 | Поздний ответ старого поиска после изменения фильтра/портала | Старая ревизия не перезаписывает текущую выборку                                  | НЗ                                 | НЗ     |
| T27 | Клавиатура, фокус подтверждения, объявления прогресса        | Действия доступны без мыши; фокус управляемый, статус не передаётся только цветом | НЗ                                 | НЗ     |
| T28 | Сборка для подкаталога и публичные переменные окружения      | JS/CSS доступны по правильным URL; клиентская сборка не содержит секретов         | НЗ                                 | НЗ     |

## Протокол одного прогона

- Run ID и дата.
- Версия кода/commit и версия приложения.
- Среда: mock либо согласованный тестовый портал; браузер и режим хранилища.
- ID тестов и результат по каждому.
- Входной набор и ожидаемые ID; для живого теста — ссылка на согласованный состав и лимиты.
- Ожидаемые команды, фактические результаты и контрольные ID, которые должны остаться.
- Логи/скриншоты без токенов и лишних персональных данных.
- Ошибки, неизвестные результаты, ограничения и необходимые исправления.

## Журнал прогонов

### local-lead-entity-2026-09-11

- Среда: локальные искусственные данные и fake-ответы официального SDK, Node.js 24.18.0; сеть и Bitrix24 не использовались, колонка Bitrix остаётся НЗ.
- Итог после независимого ревью: `npm test` — 290/290 тестов в 25 файлах; typecheck, lint, форматирование, production build, PHP lint, `git diff --check` и байтовое совпадение корневых PLAN/PRODUCT с зеркалами прошли.
- T01/T02/T03/T04/T05: для лидов проверены пустой фильтр, 0/3 000/3 001, полный фиксированный snapshot, исключения и отсутствие смешивания со сделками. Общие PASS-статусы строк сохраняются.
- T07/T08 scoped evidence: checkpoint и Web Locks разделены по порталу, пользователю и типу сущности; schema 1 принимает `deal`/`lead` и отклоняет неизвестный тип. Полная смена настоящего портала/пользователя остаётся НЗ, поэтому T08 не переводится в PASS.
- T20/T25 scoped evidence: lead CSV использует общий проверенный механизм экранирования и защиты от формул; отдельные тесты проверяют состав колонок, имя файла и отказ при смешении сущностей. Для lead item проверен отказ при неизвестном статусе; отдельные malformed-тесты справочника лидов не выполнялись. Проверены сортировка, пагинация и отбор `SEMANTICS: F` или `EXTRA.SEMANTICS: failure`.
- T26 scoped evidence: смена сущности сбрасывает preview и справочники, поздний ответ сделок не заменяет актуальные лиды. Смена фильтра уже покрыта reducer/hook-тестами; портал не проверялся, строка остаётся НЗ.
- UI-flow лидов проходит поиск → исключение → подтверждение → demo-удаление и проверяет тексты выбранной сущности. CSV проверен отдельно доменными и компонентными тестами. Deal-тесты сохранены.
- Production-сборка использует относительные assets. Vite сообщает неблокирующее предупреждение о JS chunk 503,30 kB; реальный SprintHost/iframe остаётся частью T22/T28.
- Независимое ревью диапазона `20656b8..259b5bb`: Critical/Important не найдено; reviewer отдельно выполнил 119 тестов в пяти файлах. Исправлены Minor-замечания: восстановлена runtime-проверка поля даты в подтверждении, исправлен текст исключения сделок, уточнены границы доказанного покрытия.
- Дополнительные регрессии ревью: недопустимое поле даты отклоняется при создании и проверке snapshot; поздний checkpoint сделок игнорируется после переключения на лиды при тех же сервисах; App в режиме чтения показывает lead preview и CSV, но не подтверждение даже с mock-адаптером, способным удалять.
- Повторное независимое ревью исправлений: 78 тестов в трёх файлах прошли; новых замечаний нет, код готов к локальному объединению. Проверка настоящего портала этим выводом не заменяется.

### readonly-adapter-2026-09-10

- Среда: локальные fake-ответы официального SDK, Node.js 24.18.0; сеть и Bitrix24 не использовались, колонка Bitrix остаётся НЗ.
- Итог: `npm test` — 252/252 теста в 23 файлах. Контрактные тесты проверяют точные read-only методы/параметры, offset-пагинацию воронок с сохранением основной `id=0`, документированную форму profile без offset и пустую зону, динамические воронки и semantics стадий, активных пользователей, IANA/DST, строгий datetime с явной зоной, потоковые страницы стадий/пользователей/сделок, дедупликацию, границы справочников 50/51 и сделок 0/3 000/3 001, раннее закрытие генератора.
- T20 Mock PASS: React выводит внешние названия как текст; URL карточки создаётся только из проверенного HTTPS-origin и числового ID, CSV-тесты покрывают формулы, управляющие символы, кавычки, переносы и кириллицу.
- T25 Mock PASS: некорректные profile/category/stage/user/deal payload отклоняются кодом `invalid-bitrix-response`; ошибки SDK очищаются до стабильного кода. Шлюз до вызова SDK запрещает метод вне read-only allowlist.
- Scoped evidence T01: real-адаптер отдельно проверен на 3 000/3 001 уникальный ID и дубли, но производительность и ответы настоящего портала не измерялись.
- Scoped evidence T22: top-level выбирает demo, iframe выбирает SDK без mock-fallback, контекст загружается до возврата runtime, неуспешная инициализация закрывает SDK и показывает безопасный экран. Ошибки соединения, REST и контракта сохраняют разные безопасные коды без внешних деталей. Настоящие iframe/postMessage/scopes и SprintHost не проверены.
- Scoped evidence T28: SDK установлен как точная версия, production-сборка использует относительные assets; проверка bundle и секретов зафиксирована в `docs/STATUS.md`. SprintHost остаётся НЗ.

### local-operation-2026-09-10

- Итог: `npm test` — 203/203 теста в 19 файлах; typecheck, lint, production build, PHP lint, проверка diff и совпадения корневых PLAN/PRODUCT с зеркалами прошли.

- Среда: искусственные данные, Node.js 24.18.0 и Chrome 150; Bitrix24 не использовался.
- T02: OperationRunner.test.ts напрямую отклоняет пустой/превышенный набор, пустые условия, non-admin, просроченный и отсутствующий актуальный snapshot до lock/storage/transport.
- T03–T05: confirmation.test.ts проверяет версии/условия/контекст и копирование ID; DealConfirmation.test.tsx — изменённое/просроченное подтверждение; OperationFlow.test.tsx проходит поиск → исключение → подтверждение → отчёт, исключённая сделка остаётся.
- T07: синхронный запрет повторного start и использования snapshot; native Web Locks не выдаёт второй вкладке занятую блокировку во время паузы; после остановки выдаёт.
- T10/T11/T14/T15/T17: OperationRunner.test.ts проверяет результаты по ID, неизвестные ответы без retry, ошибки прав, отказ начального/sent/итогового checkpoint, паузу/остановку во время запроса. Native IndexedDB probe отклоняет транзакцию, прерванную после request success; запись отсутствует.
- T24: useDemoOperation.test.tsx проверяет StrictMode и unmount без новых отправок; старый ответ при замене сервисов не перезаписывает новую операцию.
- Scoped evidence T13: максимум три попытки, ограниченная задержка, успешный retry и постоянные ошибки проверены. Отдельного UI-индикатора ожидания REST-лимита пока нет, поэтому вся строка не PASS.
- Scoped evidence T08/T16/T20/T25/T27/T28: parser изолирует контекст и отклоняет некорректную схему/статусы; повторное открытие превращает sent в unknown без автостарта; CSV выгружает 44 выбранные строки, тесты покрывают формулы/кавычки/кириллицу и XSS; native dialog/Escape/focus проверены на 1 200/700/350 px. Живые ссылки, SDK-ответы, смена настоящего контекста, полноценное продолжение и iframe не проверены. Продолжение из T16 отложено; MVP требует нового поиска по PRODUCT P15.
- 3 000 ID проверены runner-тестом, не как измерение скорости настоящей CRM/IndexedDB. Native browser сценарий использовал 44 ID и остановку.
- Артефакты: `.superpowers/local-operation-checks/browser-result.json` и `confirmation-{1200,700,350}.png`. Промежуточный браузерный прогон прервался при hot reload; итоговый прогон стабильного кода завершился успешно.
- Независимое ревью: критических замечаний не осталось; регрессии фокуса и замены сервисов исправлены.

### mock-preview-2026-09-10

- Среда: локальный `MockBitrixAdapter`, Node.js 24.18.0, npm 11.16.0, PHP 8.5.4, Google Chrome 150.0.7871.124. Сеть Bitrix24 не использовалась; все ячейки Bitrix остаются НЗ.
- T01 Mock PASS: [MockBitrixAdapter.test.ts](../src/deals/data/MockBitrixAdapter.test.ts) проверяет 0, 1, 3 000, 3 001 и дедупликацию; [pagination.test.ts](../src/deals/ui/pagination.test.ts) проверяет 0, 1, 50 и 51. `npm test` завершился: 48 tests passed.
- Scoped local evidence для T02, но не PASS: [dealSearch.test.ts](../src/deals/domain/dealSearch.test.ts) покрывает пустой фильтр; исходники `src` не содержат DELETE-контракта, а Chrome не нашёл кнопок удаления после поиска. Исполнитель удаления отсутствует, поэтому ожидаемый результат строки T02 ещё не проверяем.
- Scoped local evidence для T05, но не PASS: [selection.test.ts](../src/deals/domain/selection.test.ts) и [DealPreview.test.tsx](../src/deals/ui/DealPreview.test.tsx) покрывают исключение. В Chrome счётчики после исключения стали `Найдено 45`, `Выбрано 44`, `Исключено 1`, после возврата — `Выбрано 45`. DELETE-команды не существует, поэтому гарантия строки T05 пока не применима и не подтверждена.
- Scoped local evidence для T26, но не PASS: [useDealSearch.test.tsx](../src/deals/state/useDealSearch.test.tsx) проверяет поздний ответ старой ревизии для mock-поиска. Смена портала не реализована и не проверялась.
- Scoped local evidence для T27, но не PASS: [App.test.tsx](../src/app/App.test.tsx) проверяет `role=status` для терминальных состояний пустого результата и превышения лимита; Chrome подтвердил 40 × 40 px цель и переключение чекбокса из её края. Управление фокусом подтверждения и объявления прогресса не реализованы.
- Scoped local evidence для T28, но не PASS: `npm run build` создал относительные assets, а Chrome открыл `/dist/index.php` через локальный PHP-сервер и получил оба ресурса из `/dist/assets/`. В `src` и собственном коде сборки не найдены `client_secret`, `refresh_token` или `crm.item.delete`; строка `dangerouslySetInnerHTML` присутствует только внутри vendor-кода React в минифицированном bundle. Публичные переменные окружения и SprintHost не проверялись.
- Browser proof: на 1 440, 1 024, 700 и 1 199/1 200/1 201 px отсутствует переполнение документа; на 1 199/1 200/1 201 px все контролы находятся внутри формы, соседние панели и поля не пересекаются. При 1 101→1 100 px сетка фильтров меняется 3→2, а сводка preview переносится над таблицей без пересечения. При 1 024 px сводка расположена выше таблицы, при 700 px фильтры имеют одну колонку и таблица — единственный горизонтально прокручиваемый блок. Проверены focus ring (3 px), поиск, исключение/возврат, переход страницы, сброс страницы новой ревизией, предупреждение stale preview и 40 × 40 px edge-click чекбокса. Артефакты: `.superpowers/sdd/2026-09-09-deal-mock-preview-implementation/task-7-screenshots/` и `.superpowers/sdd/2026-09-09-deal-mock-preview-implementation/final-fixes-screenshots/`.
- Ограничения: Bitrix24, SprintHost, iframe, CSV, подтверждение и реальные изменения CRM не проверялись. Проверка на CSS-ширине 350 px служит эквивалентом 200 % масштаба для 700 px, но не заменяет zoom в iframe Bitrix24.

Успешную сборку не записывать как успешное испытание удаления. Mock-успех не переносить автоматически в столбец Bitrix.

## Критерий выпуска

Нет открытых критических дефектов удаления, изоляции данных и авторизации. Для каждого применимого случая есть результат и доказательство в нужной среде. Известные ограничения отражены в PRODUCT и справке. Размеры живых тестов согласованы; фиктивная проверка 3 000 записей не доказывает реальную производительность API.
